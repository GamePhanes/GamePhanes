The Godot project in `/app` is a small platformer called Neon Relay.

The runner can move right and collect energy shards, but the phase jump no longer lifts the player. Repair the project so that the phase-jump input produces the intended upward movement.

Acceptance criteria:

- The project imports and launches from `/app` in headless mode.
- After moving right, the phase-jump input produces a negative vertical velocity.
- The runner can still collect all three energy shards.
- The runner can still reach the relay after collecting all three shards.
- Do not remove the existing movement, shard, or relay behavior to bypass the task.

Work only in the `/app` project. Verify the complete loop before finishing.
